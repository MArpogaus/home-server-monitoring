---
# These hold credentials and only podman reads them, as the service user.
# They still reach the container's environment: 0600 keeps them out of a
# world-readable file on the host, not out of `podman inspect`.
quadlet_service_config_modes:
  ntfy-auth.env: '0600'
  grafana.env: '0600'
