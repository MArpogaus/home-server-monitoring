[Pod]
PodName=monitoring
# 127.0.0.2: pasta maps only 127.0.0.1 into the proxy pod, so the proxy can
# reach ntfy and nothing else here.
PublishPort=127.0.0.2:3000:3000/tcp
PublishPort=127.0.0.2:3100:3100/tcp
PublishPort=127.0.0.2:9090:9090/tcp
PublishPort=127.0.0.1:8081:8081/tcp
