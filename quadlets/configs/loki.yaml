auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096
  # At info the ruler logs every query it runs, once a minute per rule. Those
  # lines quote the rules' match strings, and they land in the journal that
  # the rules read, so every |= rule matched its own evaluation log.
  log_level: warn

common:
  instance_addr: 127.0.0.1
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

limits_config:
  allow_structured_metadata: false
  retention_period: 30d

# Retention needs the compactor; without it the journal copy grows forever
# on the disk it shares with the data.
compactor:
  working_directory: /loki/compactor
  retention_enabled: true
  delete_request_store: filesystem

ruler:
  storage:
    type: local
    local:
      directory: /etc/loki/rules
  rule_path: /tmp/loki-rules
  alertmanager_url: http://127.0.0.1:9093
  enable_api: true
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2020-10-24
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h
